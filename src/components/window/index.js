import React from 'react';

import { Controls, Wrapper, Window, ImgContainer } from './index.styled';

export default () => (
  <Wrapper>
    <Controls />
    <Window>
      <ImgContainer />
    </Window>
  </Wrapper>
);
