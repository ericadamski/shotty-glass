import styled from 'styled-components';

export const Group = styled.div`
  position: fixed;
  bottom: 15px;
  right: 15px;

  display: flex;
  flex-direction: row;
  flex-wrap: nowrap;
  justify-content: space-between;
`;
