import styled from 'styled-components';
import colors from './utils/colors';

export const Wrapper = styled.div`
  background: ${colors.rgba.grey};
  width: 100vw;
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
`;
